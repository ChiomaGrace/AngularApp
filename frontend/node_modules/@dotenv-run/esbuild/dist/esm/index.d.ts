import { type DotenvRunOptions } from "@dotenv-run/core";
import type { Plugin } from "esbuild";
declare const dotenvRun: (options: DotenvRunOptions) => Plugin;
export { dotenvRun, DotenvRunOptions };
export default dotenvRun;
